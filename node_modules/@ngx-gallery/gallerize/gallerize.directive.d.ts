import { ElementRef, Renderer2, OnInit, OnDestroy } from '@angular/core';
import { Gallery } from '@ngx-gallery/core';
import { Lightbox } from '@ngx-gallery/lightbox';
import { Subject } from 'rxjs/Subject';
export declare class GallerizeDirective implements OnInit, OnDestroy {
    private _el;
    private _renderer;
    private _lightbox;
    private _gallery;
    private platform;
    observer: any;
    gallerizer$: Subject<{}>;
    /** Add images with specific classes to the gallery */
    gallerize: string;
    forClass: string;
    constructor(_el: ElementRef, _renderer: Renderer2, _lightbox: Lightbox, _gallery: Gallery, platform: Object);
    ngOnInit(): void;
    ngOnDestroy(): void;
}
